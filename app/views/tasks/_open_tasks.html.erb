<div id="tasks">
  <% if tasks.any? %>
    <%= render partial: "task_card", collection: tasks, as: :task, locals: { completed: false, hide_comment_actions: true } %>
  <% else %>
    <p>No open tasks at the moment.</p>
  <% end %>
</div>
